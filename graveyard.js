/* 

   let sumExpenses = account.expenses.reduce((number1, number2) => {
      return number1 + number2;
   })


getSummary: function() {
      alert(`${this.name}, 
      your total income is ,
      your total expenses are ${sumExpenses},
      this makes your balance .`);
   }
 


 //array that hold expenses
   expensesNumber: [600, 956],
   expensesString: ["food", "gas"],
 
 
 
getTotal: function () {
   let sumIncome = this.income.values(); //summarize income-array 
   let sumExpenses = this.expenses.values(); //summarize expenses-arraY
   const totalSum = sumIncome - sumExpenses; 
   return sumIncome, sumExpenses, totalSum;  
 }, 



 function totalExpenses () {
   for(let i = 0; i < account.expenses.lenght; i++) {
         if (typeof account.expenses[i] === Number) {
            Number(account.expenses[i])
            return Number(account.expenses.reduce((x, y) => {return x + y}))
         }
   }
}

 
let sumExpenses = 0;
for(let i = 0; i < Numbers(account.expenses.lenght); i++){
   sumExpenses += account.expenses[i];
}
 
 
   let onlyNumExp = account.expenses.filter(totalExpenses);

   function totalExpenses () {
      return typeof account.expenses === number;
}


let totalExpenses = parseFloat(`${account.expenses}`)
 console.log(totalExpenses);


 function sumExpenses () {
let totalExpenses;
for(let i = 0; i < account.expenses; i++) {
   parseInt(account.expenses[i]);
   totalExpenses += account.expenses[i];
}
return totalExpenses
}
 

function totalExpenses() {
   let sumExpenses;
   for(let i = 0; i < account.expenses.lenght; i++) {
      if (typeof account.expenses[i] === Number) {
         sumExpenses += account.expenses[i]
   }

   }
   return sumExpenses;
}
console.log(sumExpenses)




let sumExpenses = account.expenses.filter(checkNumber)}

function checkNumber (num) {
   return typeof Number}





function totalExpenses (){
   for(let i = 0; i < account.expenses.length; i++) {
      if (typeof account.expenses[i] === "string") 
      continue;}
}

console.log(`${account.expenses.forEach(totalExpenses)}`);
let sumExpenses = account.expenses.filter(totalExpenses); 




function totalExpenses(sum) {
   for (let i = 0; i < account.expenses.lenght; i++); 
   return sum = typeof account.expenses[i] === "string";
}




function totalExpenses(sum) {
   for (let i = 0; i < account.expenses.lenght; i++); 
   return sum = typeof account.expenses[i] === "string";
}




const sumExpenses = account.expenses.filter(totalExpenses);
function totalExpenses (){
   for(let i = 0; i < account.expenses.length; i++) {
      if (typeof account.expenses[i] === "string") 
      continue};
}
console.log(sumExpenses);



for(let sumIncome = 0; i < account.income.lenght; i++){
   sumIncome += account.income[i];  
}
console.log(`${i}`);
console.log(sumIncome);
   

const sumExpenses = account.expenses.filter(checkNumber);

function checkNumber() {
   for(let i = 0; i < account.expenses.length; i++)
   if (i === Number){
      return
   } 
}
console.log(sumExpenses);



function checkNumber() {
   for(let i = 0; i < account.expenses.length; i++)
   if (i == typeof "number"){
   return 
} 
}

console.log(sumExpenses);


 let sumIncome = account.income.reduce((number1, number2) => {
   return number1 + number2;
})

*/
